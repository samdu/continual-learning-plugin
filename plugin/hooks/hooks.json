{
  "description": "Continual learning stop hook — triggers incremental AGENTS.md updates",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/env PATH=\"$HOME/.bun/bin:/opt/homebrew/bin:/usr/local/bin:$PATH\" bun run ${CLAUDE_PLUGIN_ROOT}/hooks/continual-learning-stop.ts --trial"
          }
        ]
      }
    ]
  }
}
